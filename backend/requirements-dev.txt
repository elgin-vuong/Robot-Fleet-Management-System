# Development/CI-only tooling — deliberately NOT installed into the
# production Docker image (backend/Dockerfile only installs
# requirements.txt), so the runtime image doesn't carry a linter it never
# uses.
-r requirements.txt
ruff==0.16.8
